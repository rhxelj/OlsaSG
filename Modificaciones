El 5 de marzo busqué por qué no grababa si era mayorista o no el Presupuesto
Modifiqué
FilaCuatro
PresupGrabar

    y presupgrabar 


Modifiqué Header.module.css para que la letra sea más grande

el 19 de marzo

    / SistOLSA / OlsaSG / node_modules / webpack / hot$ cat log.js 

comenté todo esto, para que no saliera un error en el front
module.exports = function (level, msg) {
    // if (shouldLog(level)) {
    // 	if (level === "info") {
    // 		console.log(msg);
    // 	} else if (level === "warning") {
    // 		console.warn(msg);
    // 	} else if (level === "error") {
    // 		console.error(msg);
    // 	}
    // }
};

Ver de agregar en las condiciones de presupuesto una que se pueda escribir


